---
title: Convex Migration
description: What changed between v6 (Firebase) and v7 (Convex).
navigation:
  icon: i-lucide-hamburger
seo:
  title: Convex Migration - What Changed
  description: Side-by-side comparison of Finapp v6 (Firebase) and v7 (Convex) - backend, performance, security, tests, and architecture.
---

Side-by-side comparison of what the app looks like on `main` (v6, Firebase) vs `convex` (v7, Convex).

## Backend

| | main (Firebase) | convex |
|---|---|---|
| **Database** | Firebase Realtime Database | Convex (backend-as-code) |
| **Auth** | Firebase Auth via `nuxt-vuefire` | Better Auth + `@convex-dev/better-auth` |
| **Data access** | Real-time listeners (`getDataAndWatch`) | Imperative `client.query()` / `client.mutation()` |
| **Validation** | Firebase security rules (JSON) | Convex validators (`v.*`) + Zod schemas |
| **Server code** | None (client-only SDK) | `convex/` directory: schema, mutations, queries, HTTP, crons |
| **Exchange rates** | Client-side fetch | Server cron (daily at 6:00 UTC) + `rates` table |
| **User settings** | localStorage only | Convex `userSettings` table (baseCurrency, locale) with offline queue |
| **Schema indexes** | N/A | Composite indexes: `by_user_date`, `by_user_wallet`, `by_user_category` for efficient queries |
| **Migration** | N/A | `utils/migrate/firebase.js` &mdash; CLI to import Firebase JSON exports into Convex |
| **Account deletion** | N/A | `deleteUserDataFull()` &mdash; paginated wipe of app + auth tables |

### What Convex gives

- **Typed mutations** &mdash; full TypeScript on both sides, auto-generated API types
- **Server-side validation** &mdash; string length limits, numeric ranges, field non-emptiness, business rules (see [Validation Strategy](/en/development/validation-strategy))
- **Single WebSocket** for all data operations (queries, mutations, auth)
- **Pagination** via `.paginate()` API for large datasets (transactions)
- **Delta sync** &mdash; only changed records fetched after initial load (see [Sync](/en/development/sync))

## Store Pattern

| | main | convex |
|---|---|---|
| **Data source** | Firebase listener &rarr; `setTrns()` | Convex query &rarr; `mergeOfflineOps()` &rarr; `set()` |
| **Persist** | `deepUnref()` + immediate `localforage.setItem()` | Debounced 300ms `localforage.setItem()` (no deep clone) |
| **Offline ops** | Manual helpers in `trns/helpers.ts`, ad-hoc localStorage keys | Explicit queue: `pushSaveOp()`, `pushDeleteOp()`, `replay()` |
| **ID system** | Firebase push IDs | `local_` prefix &rarr; remapped to Convex ID after mutation |
| **Optimistic UI** | Partial (fire-and-forget) | Full: update UI &rarr; push op &rarr; fire mutation &rarr; remap ID on success |
| **Sync verification** | None | XOR hash (`fnv1a`) of transaction IDs; client &harr; server hash comparison on every sync |

The `vue-deepunref` dependency is removed. Store data uses `shallowRef`, so deep cloning on every persist was unnecessary.

## Performance

### Algorithm improvements

| What | main | convex | Impact |
|------|------|--------|--------|
| **Wallet totals** | `getWalletTotal(id)` called per wallet in `reduce` &mdash; each call filters all trns: O(W&times;N) | `getWalletsTotals()` single pass, distributes amounts via `Map`: O(N), then O(1) lookup per wallet | 5 wallets &times; 10K trns: ~50K &rarr; ~10K ops |
| **Interval bucketing** | Inline in `Item.vue`: `intervalsInRange.reduce()` filtering all trns per interval: O(N&times;I) | Extracted to `bucketTrnsByIntervals()`: single pass + binary search: O(N&nbsp;log&nbsp;I) | Year/365 days/5K trns: 1.8M &rarr; ~43K comparisons |
| **Transaction filtering** | 6 sequential `.filter()` chained via `reduce` + `walletsIds.some(w => includes(w))` O(W&sup2;) + always sorts O(N&nbsp;log&nbsp;N) | Single `.filter()` with early returns + `Set.has()` O(1) + sorting optional | 5 intermediate arrays + O(W&sup2;) &rarr; single pass + O(1); sort O(N&nbsp;log&nbsp;N) skipped when not needed |
| **Wallet lookup in getTotal** | `walletsIds.includes()`: O(W) per transfer | `new Set(walletsIds).has()`: O(1) per transfer | Linear &rarr; constant per lookup |
| **Demo data generation** | `{ ...acc }` spread in `.reduce()` + `getTransactibleIds()` called twice per iteration | Direct `acc[i] = ...` + lookups cached before loop | O(N&sup2;) &rarr; O(N) |
| **Category statistics** | `categoriesStat` eagerly computes both `grouped` and `ungrouped` on every change; `filter().at(0)` for biggest | Lazy `computed()` per variant, only accessed one executes; `.find()` stops at first match | Up to 2&times; fewer `computeCategoriesWithData` calls |
| **Vertical categories filter** | `verticalCategories.filter(c => c.value !== 0)` called twice in template (title + v-for): re-evaluated on every render | Cached in `visibleVerticalCategories` computed: filtered once, reused | 2 filter passes &rarr; 1 cached |
| **Number formatting** | `currency.js` library + `currencies.find()` O(N) called twice per format | Native `Intl.NumberFormat` cached in `Map` + `currencyMap.get()` O(1) | No library overhead; formatter created once per precision |
| **Last created trn** | `Object.keys().sort().find()` &mdash; sorts all trns O(N&nbsp;log&nbsp;N) then searches | Single O(N) pass tracking running max date | 10K trns: ~130K ops &rarr; ~10K ops |
| **Category parent check** | `categoriesForBeParent` calls `Object.values(trns).some()` O(N) per root category: O(M&times;N) | Pre-built `usedCategoryIds` Set via `for...in` O(N) + `Set.has()` O(1) per check; no intermediate arrays | 200 categories &times; 10K trns: ~2M &rarr; ~10K ops |
| **Recent categories** | `Object.keys(trns).filter().sort()` O(N&nbsp;log&nbsp;N) + `acc.some(c => c.id)` O(K&sup2;) dedup + `includes()` O(W) per category | Single O(N) pass via `Map` tracking latest date + `entries().toSorted()` O(K&nbsp;log&nbsp;K) + `Set.has()` O(1) | 10K trns: ~130K + K&sup2; &rarr; ~10K + K&nbsp;log&nbsp;K ops |
| **Transactible categories** | `.reduce()` with `acc.includes()` O(M) + `childIds.filter(!acc.includes)` O(C&times;M): O(M&sup2;) | `Map<parentId, childIds[]>` built in O(M) + `Set` dedup O(1) per check | 200 categories: ~40K &rarr; ~200 ops |
| **Transfer detection** | `transferCategoriesIds.includes(categoryId)` O(W) array scan per transaction | `trn.type === TrnType.Transfer` O(1) enum comparison | Linear &rarr; constant per check |
| **Type filter counts** | 3 separate `.filter()` passes over trnsIds (expense, income, transfer): O(3N) | Single loop with counters object: O(N) | 3 full passes &rarr; 1 pass |
| **Trn item cache in list** | `computeTrnItem(id)` called twice per item in template (item + date): O(2P) calls | Pre-computed `trnItemsMap` via `Map`: O(P) compute + O(1) lookups | 30 items: ~60 &rarr; ~30 computations per render |
| **Group totals cache** | `getTotalOfTrnsIds()` called twice per group in template (income + expense): O(2G) calls | Pre-computed `groupTotals` Map + `paginatedTotal` computed: O(G) compute + O(1) lookups | 15 groups: ~30 &rarr; ~15 total computations per render |
| **Category grouping sort** | `categories.sort()` called inside `reduce` on every child push: O(K&sup2;&nbsp;log&nbsp;K) per parent | Sort once after grouping loop, only if `length > 1`: O(K&nbsp;log&nbsp;K) per parent | 5 children: ~50 &rarr; ~12 comparisons |
| **Category grouping totals** | `getTotalOfTrnsIds(cat.trnsIds).sum` called twice per child (for push value + parent sum) | Computed once into `catTotal`, reused: single call per child | 2&times; fewer `getTotal` calls per child category |
| **Data sync** | Firebase full replacement on every update: O(N) | Delta sync: only changed records fetched &mdash; O(D) where D &laquo; N | Avoids reprocessing unchanged transactions |
| **Interval generation** | `list.unshift(current)` in while loop: O(n) per call, O(n&sup2;) total | `list.push(current)` + `list.reverse()`: O(1) per call, O(n) total | Year/365 days: ~66K shifts &rarr; ~365 pushes + 1 reverse |
| **Date duration keys** | Dynamic `{ [\`${period}s\`]: value }` &mdash; TS sees `Record<string, number>`, no type check against `Duration` | `toDuration(period, value)` returns typed `Duration` via exhaustive switch | Compile-time safety; new period variants cause errors, not silent fallback |
| **Date formatting** | Each format function called twice (`type: 'start'` + `type: 'end'`), concatenated; 3 IIFE switch blocks for period comparison | Each format function returns full string; `isSamePeriod(a, b, period)` replaces all 3 switches | ~40% fewer lines in `useGetDateRange`; no double calls |
| **Average computation** | `sum !== 0` checked 3 times + `Object.keys(items).length > 0` guard | Early `sum === 0` exit; `day` average always present (range &ge; 2 guaranteed) | Single check instead of 4 |
| **Favorite categories sort** | Inline sort duplicating parent-name + name logic (8 lines) | Reuses `compareCategoriesByParentAndName` utility (1 line) | DRY; single sort implementation to maintain |
| **Recent categories loop** | `.reduce()` iterates all sorted entries even after reaching `maxCategories` limit | `for` loop with `break` stops at limit | 200 categories, limit 16: ~200 &rarr; ~16 iterations |
| **Recent categories iteration** | `for...of Object.keys(trnsItems)` creates intermediate array | `for...in trnsItems` iterates directly on object | No intermediate array allocation for 10K+ trns |
| **Category IDs reuse** | `Object.keys(items.value)` called separately in `categoriesRootIds`, `favoriteCategoriesIds` | Both reuse `categoriesIds` computed | Keys computed once, shared across dependents |
| **Transactible IDs reuse** | `categoriesIdsForTrnValues` re-filters all categories with `!hasChildren` check | Delegates to already-computed `transactibleIds` + `.filter(id !== 'transfer')` | Avoids redundant O(M) parent scan |
| **Children IDs dedup** | `getChildrenIdsOrParent` duplicates `.filter()` logic from `getChildrenIds` | Delegates to `getChildrenIds()`, checks `.length` | Single filter implementation |

### Bundle and rendering

| What | main | convex |
|------|------|--------|
| **ECharts** | 9 components in main bundle (incl. PieChart, DataZoom, MarkPoint) | 7 components in lazy-loaded async chunk (unused removed) |
| **Theme plugin** | 81 lines with SSR inline scripts and regex replacements | 32 lines, SPA-only `appConfig` updates from localStorage |
| **Persist** | `deepUnref()` on every `localforage.setItem()` &mdash; full recursive clone | Direct write, debounced 300ms, no cloning |
| **Responsive layout** | `useWindowSize()` JS listener for sidebar | Pure CSS `sm:` / `md:` Tailwind breakpoints |
| **Container queries** | Media queries only | CSS container queries (`@xl/page:`, `@md/page:`) for layout-aware sizing |
| **Dependencies** | `currency.js` for number formatting | Native `Intl.NumberFormat` (no dependency) |
| **Fonts** | 4 families (Roboto, Roboto Condensed, Nunito, Unica One), multiple weights, cyrillic only - Google Fonts `<link>` in head | Same 4 families via `@nuxt/fonts`, cyrillic + latin |
| **Dark mode** | `import colors from 'tailwindcss/colors'` + `as any` cast | `usePreferredDark()` + hardcoded neutral, handles `system` mode |

## Security

| What | main | convex |
|------|------|--------|
| **Calculator parser** | `new Function(expression)` &mdash; code injection risk, requires `unsafe-eval` CSP | Recursive descent parser &mdash; only `+`,`-`,`*`,`/` on numeric literals |
| **Input validation** | Firebase rules (client can't verify) | Convex validators: string limits, numeric ranges, field checks |
| **Auth tokens** | Firebase-managed | Encrypted at rest (`encryptOAuthTokens: true`) |
| **Session lifetime** | Firebase default | 30 days, daily refresh |
| **Cascade deletes** | N/A (Firebase) | Paginated batches of 500 (Convex 16K write limit) |
| **Data integrity** | None enforced in code | Transfer &harr; `categoryId='transfer'`, type change normalization, name uniqueness |
| **Redirect safety** | No validation | `getSafeRedirectPath()` &mdash; only relative paths starting with `/` (not `//`) |

## Auth

| | main | convex |
|---|---|---|
| **Provider** | Firebase Auth | Better Auth |
| **Plugin** | `nuxt-vuefire` (auto-handles auth) | `plugins/convex.ts` &mdash; manual lifecycle |
| **Token flow** | Firebase SDK internal | `setAuth(fetchToken)` &rarr; `authClient.convex.token()` &rarr; `authReadyPromise` |
| **Offline** | Firebase SDK handles reconnect | Session watch ignores `error`/`isPending`, skips token fetch when offline, `online` event triggers fresh auth |
| **Auth state hint** | None | `finapp.localAuthUid` cookie (sync check, no network) |
| **Callback** | Firebase redirect | OTT flow + `ensureConvexAuth()` before SPA navigation |
| **CORS** | N/A | `APP_URL` env var + built-in `CONVEX_SITE_URL` |
| **Login** | Firebase auto-redirect | Stores redirect URL in localStorage, clears stale auth cookies before OAuth |

## Offline Queue

| | main | convex |
|---|---|---|
| **Structure** | Ad-hoc helpers in `trns/helpers.ts` | Dedicated `components/offline/` directory |
| **Files** | Scattered localStorage keys | `helpers.ts`, `replay.ts`, `replayHelpers.ts`, `storageKeys.ts`, `types.ts` |
| **Operations** | `saveTrnToAddLaterLocal()`, `removeTrnToDeleteLaterLocal()` | `pushSaveOp()`, `pushDeleteOp()` &rarr; generic for any entity |
| **Entity types** | String literals (`'wallets'`, `'trns'`) | `OfflineEntityType` const object for type-safe entity references |
| **Replay** | Manual on next load | `replayOfflineQueue()` with conflict detection, ID remapping |
| **Per-entity replay** | N/A | `replayWalletOps`, `replayCategoryOps`, `replayTrnOps`, `replaySettingsOps` &mdash; extracted testable functions |
| **Op validation** | None | `validateOpData()` with Zod schemas &mdash; corrupted ops logged and removed |
| **Merge** | None | `mergeOfflineOps(serverData, entity)` applies pending ops on top of fresh server data |

## PWA

| | main | convex |
|---|---|---|
| **Strategy** | `injectManifest` (custom service worker via `SW` env var) | `generateSW` (auto-generated by Workbox) |
| **Service worker** | Hand-written `sw.ts` | None &mdash; Workbox generates it |
| **Precaching** | Manual configuration | Automatic for all build assets |
| **Icons** | Fetched from Iconify API at runtime | Bundled at build time via `clientBundle` |

## TypeScript

| | main | convex |
|---|---|---|
| **Strict mode** | Enabled, but unresolved errors | Zero TypeScript errors |
| **Type casts** | `as any` used across codebase | Zero `as any` &mdash; replaced with type guards, `@vue-ignore`, narrow casts |
| **Transfer detection** | `transferCategoriesIds` computed array | `TrnType.Transfer` enum + discriminated union types |
| **Runtime validation** | None | Zod schemas for user settings, rates, stat config, date params |
| **Date period functions** | `getStartOf(date, string)`, `getEndOf(date, string)` &mdash; `default` fallback to day | Typed as `Period`, exhaustive `switch` with `case 'day'` &mdash; new variants cause compile errors |
| **Duration construction** | Dynamic `{ [\`${period}s\`]: value }` &mdash; TS sees `Record<string, number>` | `toDuration(period, value): Duration` &mdash; typed return via exhaustive switch |
| **Stat type mapping** | `Record<string, TrnType[]>` with `??` fallback | `Record<SeriesSlugSelected \| StatTabSlug, TrnType[]>` &mdash; all keys covered, no fallback needed |
| **Average key resolution** | `key as keyof TotalReturns` unsafe cast + `as number` | Typed `key: keyof TotalReturns` via explicit `'netIncome'` &rarr; `'sum'` mapping, no casts |
| **Currency rate fallback** | `rates[code] \|\| 1`, `wallet?.currency \|\| 'USD'` &mdash; falsy `0`/`''` triggers fallback | `rates[code] ?? 1`, `wallet?.currency ?? 'USD'` &mdash; only `null`/`undefined` triggers fallback |
| **Query param parsing** | `if (data.rangeOffset)` &mdash; falsy `0` skipped | `if (data.x !== undefined)` &mdash; zero values applied correctly |
| **Total props** | `baseCurrencyCode?: string` loose type | `baseCurrencyCode?: CurrencyCode` &mdash; consistent with `getAmountInRate` |
| **Category stat params** | `trnsItems: Record<TrnId, { categoryId?: string }>` inline type | `trnsItems: Record<TrnId, Pick<TrnItem, 'categoryId'>>` &mdash; linked to source type |
| **Sort null guard** | `sortCategoriesByAmount` checks `if (!a \|\| !b)` at runtime | Guard removed &mdash; TS already guarantees `CategoryWithData` parameters; explicit `: number` return type |

## Tests

| | main | convex |
|---|---|---|
| **Config** | No vitest config | 3 vitest projects: `unit` (node), `store` (happy-dom), `convex` (edge-runtime) |
| **Backend tests** | None | Full CRUD integration tests (wallets, categories, trns, sync hash) |
| **Calculator tests** | Basic | 46 cases (precedence, decimals, division by zero, injection safety) |
| **Extracted logic tests** | None | Wallet filter/grouping/counts (archived exclusion, available visibility), category grouping, auth middleware, offline replay (per-entity, Zod validation), stat barUtils, useStatItem |
| **Test infrastructure** | None | `setup-store.ts` (shared mocks), `setup.helper.ts` (Convex auth mock) |

## Refactoring

### Pure function extraction

Business logic moved from Vue components into testable pure functions:

- **Statistics:** `bucketTrnsByIntervals`, `computeAverageTotal`, `sortCategoriesByAmount`, `getSelectedType`, `computeBarStyle`, `formatCompactAmount`, `computeSeriesAverage`, `getTrnTypeByAmount`
- **Wallets:** `filterWalletsByCurrency`, `filterWalletsByViewType`, `groupWalletsByProperty`, `computeWalletCounts`, `sumWalletAmounts`, `getCreditAvailable`
- **Categories:** `collectCategoriesByTrns`, `flattenCategoriesWithValues`, `groupCategoriesWithValues`
- **Offline:** `remapTrnIds`, `isTrnOrphaned`, `groupOpsByEntity`, `splitCategoryOps`, `replayWalletOps`, `replayCategoryOps`, `replayTrnOps`, `replaySettingsOps`, `validateOpData`
- **Demo:** simplified to read-only &mdash; removed per-entity CRUD methods (`addDemoCategory`, `deleteDemoWallet`, etc.), only `generateDemoData()` + `isDemo`

### Component changes

**Added:** `ActionButton`, `ChipButton`, `TabsScroll`, `TextMuted`, `SettingsCard`, `TitleSection`, `ItemBody`, `BottomSheetModal`, `WalletsPageListItem`, `WalletsPageGroupHeader`, `Onboarding`, `CurrenciesPageList`, `CurrenciesItem`, `ConfigSwitch`, `GroupingToggle`, `SelectorItem`

**Removed:** `Item1`&ndash;`4`, `Tabs2`, `TabsItem1`/`3`/`4`, `TextSm1`/`2`, `Title3`/`6`/`7`/`8`, `TitleOption`, `ToggleAction`, `SwitcherTabs`, `Welcome`, `Round` (stat chart), `SectionWithCollapse`, `CurrenciesToggleDep`, `getStyles.ts`

### Component patterns

- **Class merging:** `getStyles()` utility removed &mdash; replaced with `cn()` (clsx + tailwind-merge)
- **Form inputs:** `value`/`updateValue` props &rarr; `v-model` / `defineModel`

### Composable decomposition

- **Wallets:** `useWalletContextMenu`, `useWalletDelete`, `useWalletsFilter`, `useWalletsGrouping`, `useWalletsCounts`
- **Categories:** `useCategoryContextMenu`
- **Statistics:** Symbol-based injection keys (`filterKey`, `statDateKey`, `statConfigKey`) for type-safe `provide`/`inject`
- **Stat pages:** `useStatPage` composable &mdash; extracts duplicated stat infrastructure (filter, statConfig, statDate creation + provide, activeTab, storageKey, trnsIds, maxRange) from three page components (Dashboard, Category, Wallet) into a single reusable composable. Each page passes only its entity-specific filter logic via `getTrnsFilter` callback. Script section reduced by 24&ndash;53% across the three pages
- **`useCategoryLongPress` composable:** extracted 59 lines of duplicated long-press logic from `Round2lines.vue`, `Line.vue`, and `Vertical.vue` into a shared composable. Handles long-press to create transactions from categories with proper date selection from chart intervals
- **`useStatDate` encapsulation:** navigation computed properties (`shouldShowNav`, `isAtEnd`, `isAtStart`, `isDayToday`, `shouldShowNavHome`) and direct `params.value` mutations moved from `Navigation.vue` and `chart/Wrap.vue` into `useStatDate` methods (`selectInterval()`, `resetInterval()`, `setIntervalsBy()`, `navigate()`). Navigation.vue reduced from ~54 lines of script to 5 lines
- **Duplicate `stat/date/Nav.vue` removed:** identical copy of `date/Nav.vue` was deleted &mdash; only the canonical `DateNav` component remains
- **`UiNumberStepper` component:** extracted duplicated &plusmn;/input counter pattern from `ConfigModal.vue` into a reusable UI primitive with `v-model`, `min`, `max` props
- **Stat component self-containment:** `StatAverage` consolidated three conditional `Amount` blocks into one via computed props; `StatSumWrap` and `StatChartWrap` removed redundant prop-drilling in favor of inject; `StatItem` merged two modal blocks into one via state-machine pattern
- **Config caching:** `Section2.vue` and `Line.vue` cache `statConfig.config.value.catsList.*` reads into computed properties (`isItemsBg`, `isLines`, `isRoundIcon`, `isListGrouped`, etc.) for template readability
- **`Range.vue` simplified:** replaced manual `intervalsInRange` indexing with `statDate.selectedInterval`; extracted `isIntervalSelected` computed
- **`useStatItem` cleanup:** extracted `baseTrnsIdsForSelection` computed to eliminate duplicated interval-selection logic; fixed unsafe `type.value as keyof TotalReturns` cast with `type.value ?? 'sum'` fallback; fixed chart average denominator using wrong data source (`intervalsData` &rarr; `intervalsDataWithFilteredCategories`)
- **`useCategoriesExpanded` composable:** extracted 52-line expand/collapse logic from `DetailedSection.vue` (286&rarr;234 lines)
- **Stat component rename:** `Section`/`Section2` &rarr; `RoundSection`/`DetailedSection` for clarity
- **`barUtils` extraction:** `computeBarStyle()` and `formatCompactAmount()` extracted from inline template logic into testable utilities
- **Wallet statistics consistency:** archived wallets fully excluded from all counts (total, type sums, withdrawal, available) via early `continue`; `available.isShow` requires both withdrawal and credit wallets; auto-reset filter to Total when currency switch produces empty results
- **Store sync consolidation:** `showErrorToast`/`showSuccessToast`/`showWarningToast` merged into generic `showToast(type, key, params?)`
- **Wallet constants:** `WALLET_STORAGE_KEYS` extracted from inline strings to `constants.ts`

### Semantic renaming

| main | convex |
|------|--------|
| `getTrnsIds` | `filterTrnsIds` |
| `getCategoriesWithData` | `computeCategoriesWithData` |
| `getTotalOfTrnsIds` | `computeTotalForTrnsIds` |
| `getChildsIds` | `getChildrenIds` |
| `trnFormCreate` / `Edit` / `Duplicate` | `openFormForCreate` / `Edit` / `Duplicate` |
| `getIsShowSum` | `shouldShowSum` |
| `isItTransactible` | `isTransactible` |
| `editedAt` | `updatedAt` |
| `TransferType` | `TransferSide` |
| `Selector2` (categories) | `SelectorGrid` |
| `Selector` (categories) | `SelectorTree` |
| `Checkbox` (ui) | `SwitchItem` |
| `Tabs1` (ui) | `TabsBar` |
| `Toggle3` (ui) | `ToggleControlled` |
| `Section` (stat) | `RoundSection` |
| `Section2` (stat) | `DetailedSection` |
| `isNotTransferCategory` | `shouldShowAmounts` |
| `biggestCatNumber` | `maxCategoryValues` |
| `addMarkArea` | `withMarkArea` |
| `toggleOpened` | `toggleAllCategoriesExpanded` |

### Data model

| | main | convex |
|---|---|---|
| **Category children** | Denormalized `childIds` array | Computed dynamically from `parentId` |
| **Transfer detection** | `transferCategoriesIds` (computed from category data) | `TrnType.Transfer` enum value |
| **Adjustment** | Not distinguished | `categoryId === 'adjustment'`, excluded from income/expense stats |

## UX Improvements

- **Haptic feedback** on transaction submit (mobile vibration)
- **Accessibility:** `<div>` &rarr; `<button>` in interactive components, i18n `aria-label` on icon-only buttons
- **Calculator:** fixed infinite loop on zero result, proper `0.` decimal display, rounded division results
- **Form:** amount clears immediately after submit, popover closes before edit navigation
- **Onboarding:** `Welcome` component replaced with `Onboarding` &mdash; improved layout, cookie-persisted state (`finapp.isOnboarded`)
- **Currencies page:** two sections (Used / All) with global search, O(1) lookup via `currencyMap`
- **Wallet/category detail:** edit/delete actions moved to three-dot menu
- **Wallet statistics:** archived wallets fully excluded from all filter tabs; Available tab only shown when both withdrawal and credit wallets exist; filter auto-resets to Total on currency switch if current filter is empty
- **Wallet edit guard:** redirect to `/wallets` when wallet doesn't exist
- **Toast dismiss:** click anywhere on toast notification to close it
- **Layout:** `keepalive` prop from layout slot for page transition persistence
- **Settings page:** reorganized with `SettingsCard`, language dropdown, currency picker modal, danger zone with "Delete all data"
- **Login page:** version display at bottom

## i18n

| | main | convex |
|---|---|---|
| **Error messages** | Generic or none | Per-entity error keys (`categories.errors.*`, `wallets.errors.*`, `trns.errors.*`) |
| **Form labels** | Verbose (`Category name`, `Category color`) | Concise (`Name`, `Color`, `Icon`) |
| **Onboarding** | `welcome.*` keys | `onboarding.*` with step-by-step guidance |
| **Theme** | `neutral`, `radius` | `Background color`, `Rounding`, `Appearance` |
| **Typos** | `errorChilds` | `errorChildren` |

## Code Quality

### Removed from main

- `vue-deepunref` dependency
- Old page-specific wallet composables
- Dead code: unused props, emits, functions, stale TODOs
- Deprecated types: `WalletsDirty`, `TrnItemDirty`, `TransferDeprecated`, `TrnTypeSlug`
- CSS utilities: `flex-center-col`, `absolute-center`, `layoutBase`
- `nuxt-vuefire` and `firebase` dependencies
- Firebase config files (`firebase.json`, `.firebaserc`, `pnpm-workspace.yaml`)

### Fixed

- Memory leaks: `addEventListener` without cleanup, `ResizeObserver` without disconnect
- Null spreading in 3 stores (missing `?? {}` guards)
- Emit during render in `SelectionCategoriesFast`
- Magic numbers replaced with `TrnType` enum (8 places)
- `ref<any>` replaced with proper types (4 files)
- Error layout localized (hardcoded English &rarr; i18n keys)
- Tailwind v4 CSS syntax: `[var(--name)]` &rarr; `(--name)` functional notation
- Radius uses `??` (not `||`) so `0` is valid: `radius ?? 0.375`
- CSS utility typo: `bottomSheetDrugClassesCustom` &rarr; `bottomSheetDragClassesCustom`
- ESLint: Convex-specific rules for `convex/`, layout `vue/no-multiple-template-root`
- `createLogger(prefix)` &mdash; dev-only `log`, always-on `warn` and `error`, with `[prefix]` formatting across all modules
- Hardcoded `'ru'` locale in chart formatters &rarr; dynamic locale from settings
- `Statistics.vue` empty group sections hidden with `v-show`
- `Selector.vue` `onSelectRange(value: any)` &rarr; typed `{ end: unknown, start: unknown }`
- `setWalletViewType` type fixed from `WalletType | 'total'` to `WalletViewTypes | 'total'`
- Version: v6.6.4 &rarr; v7.0.0-beta.10
- `useAmount.getAmountInBaseRate`: dead `noFormat` param removed, unnecessary `+` unary on number removed
- `useFilter`: redundant `[...array.filter()]` and `[...string.split()]` spreads removed &mdash; both methods already return new arrays
- `getUCalendarTimedDate`: `new Date(string)` parsed as UTC &rarr; `date.toDate(getLocalTimeZone())` for correct local timezone
- `useGetDateRange`: `type: 'start' | 'end'` pattern removed &mdash; format functions return full strings directly, 3 IIFE switches replaced with `isSamePeriod` helper
- `getIntervalsInRange`: `rangeOffset` made optional in `IntervalsInRangeProps` &mdash; not used by `getIntervalsInRange`, only by `calculateIntervalInRange`
- `compareCategoriesByParentAndName`: `||` &rarr; `??` for empty string fallback (consistent nullish semantics)
- `useCategoriesStore`: removed 5 unnecessary `?? {}` defensive checks &mdash; `items` is always initialized via `shallowRef`
- `useCategoryLongPress`: removed redundant `!` after optional chaining (`value?.start` already narrowed)
- `useCategoriesExpanded`: `forEach` &rarr; `for...of`; `reduce` with `as` cast &rarr; `Object.fromEntries`
- `sortCategoriesByAmount`: renamed `isP`/`isN` &rarr; `bothPositive`/`bothNegative` for readability; removed redundant `categories ??= []` (already initialized in grouping)

### Dependencies

| Removed | Added |
|---------|-------|
| `firebase` | `convex` |
| `nuxt-vuefire` | `@convex-dev/better-auth` |
| `vue-deepunref` | `better-auth` |
| `currency.js` | `convex-helpers` |
| | `convex-test` |
| | `@vueuse/nuxt` |
| | `zod` |
| | `es-toolkit` |
