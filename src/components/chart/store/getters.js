import moment from 'moment'

export default {
  /**
    * Total periods with selected filter
    *
    * @return {Number}
  */
  // numberOfTotalPeriods (state, getters, rootState, rootGetters) {
  //   const filterPeriod = state.filter.period
  //   const firstCreatedTrn = state.trns.items[rootGetters.firstCreatedTrnIdFromSelectedTrns]
  //   const firstCreatedTrnDate = moment(firstCreatedTrn.date).startOf(filterPeriod)
  //   const statFromDate = moment().startOf(filterPeriod)
  //   return statFromDate.diff(firstCreatedTrnDate, filterPeriod) + 1
  // }
}
