.popup
  position fixed
  left 0
  top 0 
  width 100%
  height 100%
  z-index 1
  overflow scroll
  scrollbar()

  &__in
    width 500px
    padding 100px 0
    margin 0 auto

  &._blur
    background alpha($color-body, .97)
    filter blur(5px)
