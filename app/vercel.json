{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter app generate",
  "outputDirectory": ".output/public",
  "ignoreCommand": "exit 1",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
