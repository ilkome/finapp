- var fonts = "Unica+One|Robotto:300,400,500,700"

mixin link(href)
  link(href=href rel="stylesheet")

mixin styles
  +link("https://fonts.googleapis.com/css?family=" + fonts)
  +link("http://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/css/toastr.min.css")
  +link("/css/styles.css")
